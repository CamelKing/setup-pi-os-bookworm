
	" ! these two lines must be on top before any keymapping
	" key mapping is done in the general key mapping session
	" at the bottom as well as the respective plugin setting
	" section
	let mapleader = ","					" remap leader key to ','
	let maplocalleader = "\\"		" add a local leader of '\'
	set timeout timeoutlen=1000
